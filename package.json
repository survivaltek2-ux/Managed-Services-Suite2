{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm --filter @workspace/siebert-services run build && pnpm --filter @workspace/partner-portal run build && pnpm --filter @workspace/connectors-portal run build && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "pnpm --filter @workspace/db run push",
    "setup:admin": "pnpm --filter @workspace/scripts run create-admin",
    "start": "node --env-file=.env ./artifacts/api-server/dist/index.cjs",
    "deploy:bootstrap": "sh ./scripts/deploy-linux.sh",
    "deploy:server": "sh ./scripts/deploy-linux.sh"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "overrides": {
      "path-to-regexp": "8.4.0",
      "picomatch@2": "2.3.2",
      "picomatch@4": "4.0.4",
      "axios": ">=1.15.0",
      "react": "19.1.0",
      "react-dom": "19.1.0",
      "@tootallnate/once": "3.0.1",
      "basic-ftp": "5.3.1",
      "fast-uri": "3.1.2",
      "fast-xml-builder": "1.1.7",
      "ip-address": "10.1.1",
      "brace-expansion@2": "2.0.3",
      "follow-redirects": "1.16.0",
      "esbuild": ">=0.25.0",
      "yaml": "2.8.3",
      "fast-xml-parser": "5.7.2",
      "uuid": "14.0.0",
      "postcss": "8.5.12"
    }
  }
}
